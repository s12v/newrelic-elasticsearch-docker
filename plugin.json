{
  "agents": [
    {
      "host": "%HOST%",
      "port": %PORT%,
      "name": "%NAME%",
      "username": "%USERNAME%",
      "password": "%PASSWORD%",
      "protocol": "%PROTOCOL%",
      "_basePath": "Optional. API path on server. Default '/'. Rename to 'basePath' if you need to customize it"
    }
  ]
}
